# Unknown World Backend - 환경변수 설정
# 이 파일을 .env로 복사하고 실제 값을 입력하세요.
# cp .env.example .env
#
# =============================================================================
# U-080 핫픽스: Vertex AI 제거 → API 키 인증 전용
# =============================================================================
# 
# Gemini API 키 하나로 모든 기능(텍스트/이미지)을 사용합니다.
# API 키 발급: https://aistudio.google.com/apikey
#
# =============================================================================

# =============================================================================
# GenAI 클라이언트 설정
# =============================================================================

# Gemini API 키 (필수)
# https://aistudio.google.com/apikey 에서 발급
# ⚠️ 주의: 이 파일(.env)은 .gitignore에 포함되어야 합니다! (RULE-007)
GOOGLE_API_KEY=your-api-key-here

# 동작 모드: mock (테스트/개발용) | real (실제 API 호출)
# - mock: 실제 API 호출 없이 모의 응답 반환 (개발/테스트용)
# - real: Gemini API 실제 호출 (기본값)
UW_MODE=real

# =============================================================================
# 서버 설정
# =============================================================================

# 백엔드 서버 포트 (RULE-011: 8011~8020 범위)
# 기본값: 8011
# PORT=8011

# =============================================================================
# 환경 구분
# =============================================================================

# 환경: development | staging | production
# - development: 로컬 개발 (상세 로깅, 디버그 정보)
# - staging: 스테이징/QA 테스트
# - production: 프로덕션/데모 배포
ENVIRONMENT=development
