# Unknown World Backend - 환경변수 설정
# 이 파일을 .env로 복사하고 실제 값을 입력하세요.
# cp .env.example .env
#
# =============================================================================
# 환경별 설정 가이드 (U-067[Mvp])
# =============================================================================
# 
# [Development] 로컬 개발 환경
#   - UW_MODE=mock (API 비용 없이 테스트) 또는 UW_MODE=real (실제 API 테스트)
#   - ENVIRONMENT=development
#   - 서비스 계정 키 파일로 인증 (GOOGLE_APPLICATION_CREDENTIALS)
#
# [Staging] 스테이징/테스트 환경
#   - UW_MODE=real
#   - ENVIRONMENT=staging
#   - Vertex AI 프로젝트 ID 명시 권장 (VERTEX_PROJECT)
#
# [Production] 프로덕션/데모 환경 ⚡ CRITICAL
#   - UW_MODE=real
#   - ENVIRONMENT=production
#   - VERTEX_PROJECT 필수 설정 (quota 관리/모니터링용)
#   - VERTEX_LOCATION=global (기본값, 최적 리전 자동 선택)
#   - Cloud Run 배포 시: 서비스 계정 키 대신 IAM 역할로 인증 권장
#
# =============================================================================

# =============================================================================
# GenAI 클라이언트 설정 (U-016, U-067)
# =============================================================================

# 동작 모드: mock (테스트/개발용) | real (실제 API 호출)
# - mock: 실제 API 호출 없이 모의 응답 반환 (개발/테스트용)
# - real: Vertex AI Gemini 모델 실제 호출 (기본값)
UW_MODE=real

# Vertex AI 서비스 계정 키 파일 경로 (로컬 개발 전용)
# - Windows: D:/Dev/unknown-world/backend/your-service-account-key.json
# - Linux/Mac: /path/to/your-service-account-key.json
# ⚠️ 주의: 
#   - 이 파일은 .gitignore에 포함되어야 합니다!
#   - Production(Cloud Run)에서는 IAM 역할로 인증하므로 불필요
GOOGLE_APPLICATION_CREDENTIALS=./your-service-account-key.json

# Vertex AI 프로젝트 ID
# - 설정하지 않으면 ADC(Application Default Credentials)에서 자동 추론
# - Production에서는 명시 권장 (quota 모니터링, 비용 추적)
# 
# [Development] 개발용 프로젝트
# VERTEX_PROJECT=your-project-dev
#
# [Production] 프로덕션 프로젝트
# VERTEX_PROJECT=your-project-prod
VERTEX_PROJECT=

# Vertex AI 리전 (U-067 수정)
# - 기본값: global (최적 리전 자동 선택)
# - 지연 최적화 필요 시 특정 리전 지정 가능:
#   - us-central1: 가장 안정적, 모델 지원 폭 넓음
#   - asia-northeast3: 한국 근접, 지연 최소화
#   - europe-west4: 유럽 지역
# 
# [Production 권장] global 사용 (기본값)
# VERTEX_LOCATION=global
#
# [지연 최적화] 특정 리전 지정
# VERTEX_LOCATION=us-central1
VERTEX_LOCATION=global

# =============================================================================
# 서버 설정
# =============================================================================

# 백엔드 서버 포트 (RULE-011: 8011~8020 범위)
# 기본값: 8011
# PORT=8011

# =============================================================================
# 환경 구분
# =============================================================================

# 환경: development | staging | production
# - development: 로컬 개발 (상세 로깅, 디버그 정보)
# - staging: 스테이징/QA 테스트
# - production: 프로덕션/데모 배포
ENVIRONMENT=development