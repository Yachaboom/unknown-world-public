# U-077[Mvp] 인벤토리 패널 스크롤 및 아이템 관리 UX 개선 실행 가이드

## 1. 개요

인벤토리 아이템이 많아질 경우에도 패널 레이아웃이 깨지지 않고 스크롤로 모든 아이템에 접근할 수 있게 개선했습니다. 또한 아이템 개수 표시, 빈 상태 힌트, 사이드바 패널 영역 분배(U-081 흡수)를 함께 처리했습니다.

**예상 소요 시간**: 5분

**의존성**:
- 의존 유닛: U-011[Mvp] (인벤토리 DnD 기본), U-049[Mvp] (레이아웃/스크롤 설계)
- 선행 완료 필요: 없음

---

## 2. 빠른 시작 (Quick Start)

### 2.1 환경 준비

```bash
cd d:\Dev\unknown-world\frontend
pnpm install
```

### 2.2 즉시 실행

```bash
pnpm dev
```

### 2.3 첫 화면/결과 확인

- 브라우저에서 `http://localhost:8001/` 접속
- 프로필 선택 후 게임 화면 진입
- 성공 지표:
  - 좌측 사이드바에 Inventory, Quest, Rule Board 패널 모두 보임
  - 인벤토리 헤더에 아이템 개수 표시 (예: "INVENTORY (4)")
  - 모든 패널 헤더가 항상 보임

---

## 3. 핵심 기능 시나리오

### 시나리오 A: 인벤토리 스크롤 동작 확인

**목적**: 아이템이 패널 높이를 초과할 때 내부 스크롤이 활성화되는지 확인

**실행**:
1. 브라우저 DevTools 콘솔(F12)에서 다음 코드 실행하여 테스트 아이템 20개 추가:
```javascript
import('/src/stores/inventoryStore.ts').then(m => {
  const items = Array.from({length: 20}, (_, i) => ({
    id: `test-${i+1}`,
    name: `테스트 아이템 ${i+1}`,
    icon: ['🗡️','🛡️','🔮','💎','📜','🧪'][i%6],
    quantity: i+1,
  }));
  m.useInventoryStore.getState().addItems(items);
});
```
2. 인벤토리 패널 내부에서 마우스 휠로 스크롤
3. 하단에 잘린 아이템이 스크롤로 접근 가능한지 확인

**기대 결과**:
- 인벤토리 패널 내부에서 휠 스크롤이 동작
- 스크롤바가 표시되지 않음 (Q2: Option C)
- 모든 아이템에 접근 가능
- 헤더에 총 아이템 개수 표시 (예: "INVENTORY (24)")

**확인 포인트**:
- ✅ 패널 내부에서만 스크롤 발생 (사이드바 전체 스크롤 아님)
- ✅ 스크롤바 숨김 (scrollbar-width: none)
- ✅ Quest/Rule Board 패널 위치 고정 (인벤토리 스크롤 영향 없음)

---

### 시나리오 B: DnD + 스크롤 호환성

**목적**: 스크롤 컨테이너 내에서 드래그 앤 드롭이 정상 동작하는지 확인

**실행**:
1. 인벤토리에 아이템이 15개 이상 있는 상태에서
2. 상단 아이템을 마우스로 8px 이상 드래그하여 드래그 모드 활성화
3. 드래그한 채로 Scene Canvas의 핫스팟 위로 이동
4. 드롭 후 인벤토리 상태 확인

**기대 결과**:
- 드래그 오버레이가 커서를 따라 이동
- 원본 아이템이 반투명 처리 (opacity: 0.3)
- 드롭 후 아이템이 원래 위치로 복귀
- 콘솔에 에러 없음

**확인 포인트**:
- ✅ 스크롤 중에도 드래그 시작 가능
- ✅ 드래그 오버레이가 스크롤 영역 밖에서도 표시됨
- ✅ 드롭 후 인벤토리 상태 정상 유지

---

### 시나리오 C: 빈 상태 UI (Q3: Option B)

**목적**: 아이템 0개일 때 힌트 텍스트가 표시되는지 확인

**실행**:
1. DevTools 콘솔에서 인벤토리 초기화:
```javascript
import('/src/stores/inventoryStore.ts').then(m => {
  m.useInventoryStore.getState().reset();
});
```
2. 인벤토리 패널 확인

**기대 결과**:
- 📦 아이콘 표시
- "[ 아이템 없음 ]" 텍스트
- "장면을 탐색하여 아이템을 찾으세요" 힌트 텍스트 (이탤릭, 연한 색)
- 헤더 타이틀: "INVENTORY" (개수 없음)

**확인 포인트**:
- ✅ 빈 상태 아이콘 + 텍스트 + 힌트 표시
- ✅ 헤더에 개수 미표시

---

### 시나리오 D: 사이드바 패널 영역 분배 (U-081 흡수)

**목적**: Quest/Rule Board에 많은 항목이 있어도 Inventory 패널의 최소 가시성이 보장되는지 확인

**실행**:
1. DevTools 콘솔에서 Quest 10개, Rule 8개 추가:
```javascript
import('/src/stores/worldStore.ts').then(m => {
  const quests = Array.from({length: 10}, (_, i) => ({
    id: `q-${i}`, label: `테스트 퀘스트 ${i+1}`, is_completed: i < 3
  }));
  const rules = Array.from({length: 8}, (_, i) => ({
    id: `r-${i}`, label: `테스트 규칙 ${i+1}`, description: `규칙 ${i+1} 설명`, is_active: true
  }));
  m.useWorldStore.setState({ quests, activeRules: rules });
});
```
2. 좌측 사이드바에서 모든 패널 확인

**기대 결과**:
- Inventory 패널이 최소 120px 높이 유지
- Quest 패널이 max-height: 200px로 제한되고 내부 스크롤 가능
- Rule Board 패널이 max-height: 200px로 제한되고 내부 스크롤 가능
- 모든 패널 헤더("INVENTORY", "QUEST", "RULE BOARD")가 항상 보임

**확인 포인트**:
- ✅ Inventory min-height 보장 (120px)
- ✅ Quest/Rule Board max-height 제한 (200px)
- ✅ Quest/Rule Board 내부 스크롤 (thin 스크롤바)
- ✅ 모든 패널 헤더 상시 노출

---

## 4. 실행 결과 확인

### 4.1 성공/실패 판단 기준

**성공**:
- ✅ 인벤토리 아이템 15+ 개에서 내부 스크롤 동작
- ✅ 스크롤바 숨김 (Q2: Option C)
- ✅ DnD가 스크롤 컨테이너와 호환
- ✅ 헤더에 아이템 개수 동적 표시
- ✅ 빈 상태에 힌트 텍스트 표시 (Q3: Option B)
- ✅ Quest/Rule Board max-height + 내부 스크롤
- ✅ Inventory min-height 보장
- ✅ 린트/타입체크 통과

**실패 시 확인**:
- ❌ 인벤토리 스크롤 안됨 → `.inventory-panel-content`의 `overflow-y: auto` 확인
- ❌ 사이드바 전체 스크롤 → `.sidebar-left`의 `overflow: hidden` 확인
- ❌ Quest/Rule 패널이 확장됨 → `.panel-quest`, `.panel-rule-board`의 `max-height` 확인
- ❌ DnD 오류 → dnd-kit DndContext가 App 최상단에 있는지 확인

---

## 5. 문제 해결 (Troubleshooting)

### 5.1 일반적인 오류

**오류**: 인벤토리 패널에서 스크롤이 안 됨
- **원인**: `.inventory-panel-content`에 `overflow: hidden`이 남아있음
- **해결**: CSS에서 `overflow-y: auto`로 변경 확인

**오류**: 스크롤바가 보임
- **원인**: `scrollbar-width: none` 또는 `::-webkit-scrollbar` 누락
- **해결**: `.inventory-panel-content`에 세 가지 스크롤바 숨김 속성 확인

**오류**: Quest/Rule 패널이 과도하게 확장
- **원인**: `max-height` 미적용 또는 `panel-quest`/`panel-rule-board` className 누락
- **해결**: App.tsx에서 Panel의 className prop 확인

### 5.2 환경별 주의사항

- **Windows**: `scrollbar-width: none`은 Firefox/Chrome 모두 지원. `-ms-overflow-style: none`은 레거시 Edge용.
- **macOS**: 오버레이 스크롤바가 기본이므로 추가 설정 불필요할 수 있음.

---

## 6. 적용된 변경 사항 요약

### 페어링 결정

| 질문 | 결정 | 설명 |
|------|------|------|
| Q1: 정렬/필터 | Option A | MVP에서는 스크롤만, MMP에서 정렬/필터 |
| Q2: 스크롤바 | Option C | 스크롤바 숨기고 터치/휠만 지원 |
| Q3: 빈 상태 | Option B | 아이템 획득 힌트 포함 |

### CSS 변경 (`frontend/src/style.css`)

1. **인벤토리 스크롤**: `.inventory-panel-content`에 `overflow-y: auto` + 스크롤바 숨김
2. **빈 상태 힌트**: `.inventory-empty-hint` 스타일 추가
3. **Inventory min-height**: `.panel-inventory { min-height: 120px }`
4. **Quest/Rule max-height**: `.panel-quest`, `.panel-rule-board`에 `max-height: 200px` + 내부 스크롤
5. **반응형**: `@media (max-height: 768px)` 조건으로 높이 조정

### App.tsx 변경

- Inventory Panel: `className="panel-inventory flex-1"`, 동적 타이틀 `t('inventory.count', { count })`
- Quest Panel: `className="panel-quest"`
- Rule Board: `className="panel-rule-board"`

### i18n 변경

- `inventory.count`: "Inventory ({{count}})"
- `inventory.empty_hint`: "장면을 탐색하여 아이템을 찾으세요" / "Explore scenes to find items"
